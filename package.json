{
  "name": "canrfcemail",
  "version": "0.0.1",
  "license" : "MIT",
  "readmeFilename": "README.md",
  "description": "returns `true` for known valid addresses and returns `false` for some invalid email addresses.",

  "repository": {
    "type": "git",
    "url": "https://github.com/iambumblehead/canrfcemail.git"
  },
  "keywords": [
    "email",
    "validation"
  ],
  "devDependencies" : {
      "jasmine-node" : ">=1.3.0"
  },
  "scripts" : {
      "test" : "node node_modules/jasmine-node/bin/jasmine-node --verbose test/"
  }
}
